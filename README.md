libmcc
---
A header-only C++ library provides a foundational set of data structures and utilities for reading, writing, and manipulating cache files and runtime memory layouts used in MCC. 

This library is intended for developers creating tools for modding, analyzing, or extending the capabilities of MCC.
